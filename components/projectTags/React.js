import ReactIcon from '../icons/ReactIcon';
import ProjectTagWrapper from '../ProjectTagWrapper';

const ReactTag = () => {
  return (
    <ProjectTagWrapper color="react">
      <ReactIcon />
      <span>React</span>
    </ProjectTagWrapper>
  );
};

export default ReactTag;
